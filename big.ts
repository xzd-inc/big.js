class Big {
  /* 基础配置 */
  static DP = 20
  static RM = 1
  static MAX_DP = 1E6
  static MAX_POWER = 1E6
  static NE = -7
  static PE = 21
  static STRICT = false

  /* 报错信息 */
  static NAME = '[big.js] '
  static INVALID = Big.NAME + 'Invalid '
  static INVALID_DP = Big.INVALID + 'decimal places'
  static INVALID_RM = Big.INVALID + 'rounding mode'
  static DIV_BY_ZERO = Big.NAME + 'Division by zero'

  
}